rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Bazaar logos, product photos, user avatars isolated by user UID
    match /bazaars/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}